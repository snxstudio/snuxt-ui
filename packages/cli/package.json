{
  "name": "snuxt-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "snuxt-ui": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "prompts": "^2.4.2",
    "picocolors": "^1.1.0"
  },
  "devDependencies": {
    "@types/prompts": "^2.4.9",
    "tsup": "^8.3.0",
    "vitest": "^2.1.0"
  }
}
