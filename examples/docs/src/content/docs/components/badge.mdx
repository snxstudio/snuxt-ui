---
title: Badge
description: A small label for status or categorization.
---

import { Tabs, TabItem } from '@astrojs/starlight/components'
import { BadgeDemo } from '../../../demos/BadgeDemo'
import ComponentPreview from '../../../components/ComponentPreview.astro'
import PropsTable from '../../../components/PropsTable.astro'

A compact badge component for displaying status, labels, or counts. Supports multiple visual variants to convey different meanings.

## Preview

<Tabs>
  <TabItem label="Preview">
    <ComponentPreview>
      <BadgeDemo client:load />
    </ComponentPreview>
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { Badge } from '@snuxt-ui/react'

    export function BadgeDemo() {
      return (
        <div className="flex flex-wrap gap-2">
          <Badge variant="default">Default</Badge>
          <Badge variant="secondary">Secondary</Badge>
          <Badge variant="outline">Outline</Badge>
          <Badge variant="destructive">Destructive</Badge>
        </div>
      )
    }
    ```
  </TabItem>
  <TabItem label="Angular">
    ```ts
    import { Component } from '@angular/core';
    import { SnxBadgeComponent } from '@snuxt-ui/angular';

    @Component({
      selector: 'app-badge-demo',
      standalone: true,
      imports: [SnxBadgeComponent],
      template: `
        <div class="flex flex-wrap gap-2">
          <snx-badge variant="default">Default</snx-badge>
          <snx-badge variant="secondary">Secondary</snx-badge>
          <snx-badge variant="outline">Outline</snx-badge>
          <snx-badge variant="destructive">Destructive</snx-badge>
        </div>
      `,
    })
    export class BadgeDemoComponent {}
    ```
  </TabItem>
</Tabs>

## Installation

<Tabs>
  <TabItem label="CLI">
    ```bash
    npx snuxt add badge
    ```
  </TabItem>
  <TabItem label="Manual (React)">
    Copy these files into your project:
    - `css/components/badge.css`
    - `react/badge.tsx`
  </TabItem>
  <TabItem label="Manual (Angular)">
    Copy these files into your project:
    - `css/components/badge.css`
    - `angular/badge.ts`
  </TabItem>
</Tabs>

## Usage

```tsx
import { Badge } from '@snuxt-ui/react'
```

### Default

```tsx
<Badge variant="default">Default</Badge>
```

### Secondary

```tsx
<Badge variant="secondary">Secondary</Badge>
```

### Outline

```tsx
<Badge variant="outline">Outline</Badge>
```

### Destructive

```tsx
<Badge variant="destructive">Destructive</Badge>
```

### Inline with Text

```tsx
<p>
  Status: <Badge variant="default">Active</Badge>
</p>
```

## API Reference

<PropsTable props={[
  { name: 'variant', type: '"default" | "secondary" | "outline" | "destructive"', default: '"default"', description: 'The visual style of the badge.' },
  { name: 'className', type: 'string', description: 'Additional CSS class names.' },
  { name: 'children', type: 'ReactNode', description: 'Badge content.' },
]} />
